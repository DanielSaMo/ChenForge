@top Program { Newline* (Statement Newline+)* Statement? }

Statement {
  Declaration LineComment? | LineComment
}

Declaration {
  entityDecl | attributeDecl
}

entityDecl[@name=entityDecl] {
  Entity Identifier
}

attributeDecl[@name=attributeDecl] {
  Attribute EntityRef AttributeName ("," AttributeName)* AttributeType (CompositionArg | CardinalityArg)?
}

EntityRef { Identifier }

AttributeName { Identifier }

CompositionArg { Identifier }

CardinalityArg {
  "(" CardinalityValue "," CardinalityValue ")"
}

CardinalityValue {
  Number
}

@tokens {
  Entity { "entity" }
  Attribute { "attribute" }

  AttributeType {
    "PK" | "UK" | "OP" | "DR" | "SP" | "CP" | "MV"
  }

  Number {
    "-"? $[0-9]+ | "n"
  }

  Identifier {
    $[A-Za-z_\u{00C0}-\u{10FFFF}]
    $[A-Za-z0-9_\u{00C0}-\u{10FFFF}]*
  }

  LineComment { "//" ![\n]* }
  Newline { $[\n]+ }
  WhiteSpace { $[ \t\r]+ }
}

@skip { WhiteSpace }
